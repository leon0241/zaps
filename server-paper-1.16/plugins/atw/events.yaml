# All the Webhooks — Event Rules
# Event rules below are commented out so they do not fire on installation.
# Uncomment the events you want to use.

defaults:
  enabled: false
  webhook: default
  # webhook-username: "Events"   # optional Discord display name for these events
  message: generic
  require-permission: null

worlds:
  world_nether:
    enabled: false

events:
  # world.load:
  #   message: world_load
  #   conditions:
  #     world.name:
  #       not:
  #         - "world_nether"
  #         - "world_the_end"

  # player.join:
  #   message: player_join

  # player.quit:
  #   message: player_quit

  # player.break.block:
  #   require-permission: allthewebhooks.track.break
  #   message: block_break
  #   conditions:
  #     player.gamemode:
  #       equals: SURVIVAL

  # player.break.block.*:
  #   message: generic_block_break

  # player.death:
  #   message: player_death

  # player.death.attack.lava:
  #   message: lava_death

  # entity.damage.player:
  #   message: player_damaged
  #   conditions:
  #     damage.amount:
  #       greater-than: 5
  #     damage.cause:
  #       not:
  #         - FALL
  #         - VOID
